[project]
name = "hackathon-todo-app"
version = "0.1.0"
description = "A spec-driven console todo application in Python"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
todo = "src.main:main"
